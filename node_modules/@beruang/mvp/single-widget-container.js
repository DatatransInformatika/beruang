export default class SingleWidgetContainer {
  constructor(el) {
    this._container = el;
  }

  setWidget(el) {
    let c;
    while( (c = this._container.firstChild) ) {
        this._container.removeChild(c);
    }
    this._container.appendChild(el);
  }
}
