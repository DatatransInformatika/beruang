import {html, PolymerElement} from '@polymer/polymer/polymer-element.js';
import '@webcomponents/shadycss/entrypoints/apply-shim.js';
import {BeruangBehavior} from '@beruang/beruang-behavior.js';
import {BrownBehavior} from '@beruang/brown/brown-behavior.js';
import '@beruang/brown/brown-style.js';

class BrownLabel extends BrownBehavior(BeruangBehavior(PolymerElement)) {
  static get template() {
    return html`
      <style  include="brown-style">
        :host {
          padding:5px 5px;
          font-family: var(--label-font-family);
          font-size: var(--label-font-size);
        }
      </style>
      <slot></slot>
    `;
  }
}

window.customElements.define('brown-label', BrownLabel);
